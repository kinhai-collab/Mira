# Core FastAPI and server dependencies
fastapi==0.119.0
uvicorn==0.37.0
starlette==0.48.0
python-multipart==0.0.20
mangum==0.17.0
awslambdaric==2.0.4

# HTTP clients and networking
httpx==0.28.1
httpcore==1.0.9
requests==2.32.5
h11==0.16.0
h2==4.3.0
hpack==4.1.0
hyperframe==6.1.0
httptools==0.7.1
httplib2==0.31.0
websockets==13.1
yarl==1.22.0
multidict==6.7.0
jiter==0.11.1
anyio==4.11.0
sniffio==1.3.1

# Authentication and OAuth
google-auth==2.41.1
google-auth-oauthlib==1.2.3
google-auth-httplib2==0.2.1
google-api-python-client==2.187.0
google-api-core==2.28.1
googleapis-common-protos==1.72.0
requests-oauthlib==2.0.0
oauthlib==3.3.1
PyJWT==2.10.1

# Supabase
supabase==2.24.0
gotrue==2.12.4
postgrest==2.24.0
storage3==2.24.0
realtime==2.24.0
supabase-auth==2.24.0
supabase-functions==2.24.0

# OpenAI
openai==2.7.1

# Voice and audio processing
elevenlabs==2.18.0
pydub==0.25.1
webrtcvad==2.0.10

# Calendar and date handling
icalendar==6.3.2
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2

# HTML parsing
beautifulsoup4==4.14.2
lxml==6.0.2
soupsieve==2.8
# Note: html5lib and webencodings removed - code uses built-in html.parser
# lxml is kept as a faster alternative parser option

# Payments
stripe==10.11.0

# Pydantic and validation
pydantic==2.12.4
pydantic_core==2.41.5
annotated-types==0.7.0

# Environment and configuration
python-dotenv==1.1.1

# Utilities
# Note: certifi, charset-normalizer, idna, urllib3 are transitive dependencies of requests/httpx
# uritemplate is transitive dependency of google packages
# packaging, pyparsing are transitive dependencies of various packages
certifi==2025.10.5
charset-normalizer==3.4.4
idna==3.11
urllib3==2.5.0
uritemplate==4.2.0
packaging==25.0
pyparsing==3.2.5

# Cryptography
cryptography==46.0.3
cffi==2.0.0
pycparser==2.23
rsa==4.9.1
pyasn1==0.6.1
pyasn1_modules==0.4.2

# Type hints
# Note: typing_extensions is transitive dependency of pydantic and other packages
typing_extensions==4.15.0

# Protocol buffers
protobuf==6.33.0
proto-plus==1.26.1

# Development and tooling
# Note: watchfiles is transitive dependency of uvicorn (for --reload)
# exceptiongroup, propcache are transitive dependencies
# cachetools might be transitive dependency
watchfiles==1.1.1
exceptiongroup==1.3.0
propcache==0.4.1

# Vector database and embeddings (removed - not currently used)
# chromadb==0.4.24
# sentence-transformers==2.7.0

# LangChain (removed - not currently used in codebase)
# If needed in the future, install compatible versions:
# langchain>=0.3.0
# langchain-community>=0.3.0
# langchain-openai>=0.2.0 (supports openai 2.x)

# Note: ffmpeg is a system binary, not a Python package
# It should be installed separately on the system if needed for audio conversion
# zoneinfo is part of Python 3.9+ standard library, no need to install
